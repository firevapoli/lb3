numbers = input("Введите числа через пробел: ")
numbers_list = numbers.split()

min_number = float(numbers_list[0])

 number in numbers_list:
    if float(number) < min_number:
        min_number = float(numberprint("Наименьшее число:", min_number)
